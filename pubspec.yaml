name: rcl_transactionlistapp
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  equatable: ^2.0.7
  json_annotation: ^4.8.1
  connectivity_plus: ^6.1.4
  mockito: ^5.4.5
  shared_preferences: ^2.5.3
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  json_serializable: ^6.8.0
  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true

  assets:
    - .env


